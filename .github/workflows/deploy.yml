name: Deploy to GitHub Pages

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: kelvinbward/.github/.github/workflows/standard-static-deploy.yml@main
    with:
      build-command: 'npm run build'
      output-path: './dist'
